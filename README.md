# 红色座机 · 给 AI 装一部电话

**用一台 33 块钱的红色座机，让 AI 编程助手"接电话"——摘机说话，它干完活会打回来。**

> 一个把上世纪的话机接进本世纪 AI 工作流的小项目。
> 全本地运行，不依赖任何云端语音服务；总硬件成本不到 300 元。

![本地控制台](docs/控制台截图.png)

**自带一个本地网页控制台**：看服务状态、看它听到的话、启停服务、试响一声、改设置。
零依赖 —— 只用 Node 内置模块，`node web/server.mjs` 就能开，不装任何前端框架。

```bash
cd 源码/phone-companion
cp phone.config.example.json phone.config.json   # 然后改里面的 workspace
node web/server.mjs                              # 打开 http://127.0.0.1:8932
```

---

## 目录结构

```
.
├── README.md                    你正在看的这份
├── LICENSE
├── 源码/phone-companion/        ★ 可运行的全部代码
│   ├── src/                     8 个核心模块（音频/SIP/状态机/会话监听/注入/识别…）
│   ├── web/                     本地网页控制台（零依赖）
│   ├── 工具/                     验证、诊断、网关配置、调试脚本
│   ├── 产物/缓存音频/            预渲染的提示音（μ-law 裸数据）
│   ├── ht801.py                 网关配置工具（inspect / configure / set / restore）
│   ├── 启动.mjs                  一键启动器
│   └── phone.config.example.json 配置模板
├── 工具/                        网卡修复脚本（USB 网卡拔插后丢静态 IP 时用）
└── docs/                        项目简述、开发历程与踩坑、TTS 调研、历史方案
```

> 第三方组件**不随仓库分发**：识别引擎（CapsWriter-Offline）请自行 clone，见"快速开始"。
> 本项目的 SIP/音频/状态机全部是自己实现的，参考实现（codex-redline）的代码一行都没有复制。

---

## 一、它解决什么问题

### 动机

用 AI 编程助手的人都会遇到一个尴尬：**你被绑在键盘前。**

想交代一件事，得先把手从别处挪开、切回窗口、敲字、发送。对于"临时想到一句"的场景，
这个动作链条太长——尤其是在你手上正拿着相机、拿着工具、或者刚躺下的时候。

**我们不想再造一个语音助手 App，而是想让"说话"这件事有个物理载体。**

### 为什么是电话

电话天然满足三个条件：

1. **拿起就能说** —— 摘机是一个不可误触的物理动作，比"嘿，某某"这类唤醒词可靠得多
2. **放下就结束** —— 挂机是明确的"我说完了"信号，不需要你按任何按钮
3. **它天生就是双向的** —— 有听筒能听、有话筒能说、还能响铃

第 3 条尤其关键：**普通麦克风只能输入，而电话能主动找你。**
这正是这个项目最核心的设计：**AI 干完活，电话响铃把你叫回来。**

### 最终形态

```
你拿起听筒          →  听到一声"嘀"（可以说了）
你对着它说话        →  本地实时转成文字
你放下听筒          →  文字自动提交给 AI 助手
AI 开始干活         →  你可以离开去干别的
AI 干完了           →  电话响铃 —— 回来看看结果
```

---

## 二、为什么不用现成方案

| 现成方案 | 为什么不用 |
|---|---|
| 智能音箱 | 需要唤醒词、依赖云端、不能"挂机提交"、更不会主动响铃叫你 |
| 手机语音输入法 | 结果直接注入焦点窗口，程序拿不到文本流；且依赖手机在线 |
| 云端语音 API | 说话内容要上传；有月费；离线不可用 |
| USB 麦克风 | 只能输入，不能主动通知；"开始/结束"要靠按键 |

**这个项目的取舍是：用最土的硬件（模拟话机），换最硬的体验（物理动作即交互）。**

---

## 三、系统架构

```
┌─────────────┐  RJ11   ┌──────────────┐   SIP/RTP    ┌──────────────────────────┐
│  红色座机    │◄───────►│  FXS 语音网关  │◄───────────►│  电脑（本机服务）          │
│             │  馈电     │ (模拟电话转IP) │   以太网      │                          │
└─────────────┘  铃流     └──────────────┘             │  ┌────────────────────┐  │
                                                       │  │ 电话服务             │  │
                                                       │  │ · SIP 信令 :5090    │  │
                                                       │  │ · RTP 音频 :15004   │  │
                                                       │  │ · G.711 μ-law 编解码 │  │
                                                       │  │ · 状态机/提示音/回铃  │  │
                                                       │  └──┬──────────┬───────┘  │
                                                       │     │          │          │
                                                       │  16k PCM     文字        │
                                                       │     ▼          ▼          │
                                                       │  ┌────────┐ ┌──────────┐  │
                                                       │  │本地识别 │ │ 注入输入框│  │
                                                       │  │(离线ASR)│ │ 并提交    │  │
                                                       │  └────────┘ └────┬─────┘  │
                                                       │                  ▼        │
                                                       │            AI 助手处理      │
                                                       │                  │        │
                                                       │        回复完成 │        │
                                                       │        ┌─────────┘        │
                                                       │        ▼                  │
                                                       │  ┌──────────────────┐     │
                                                       │  │ 会话监听器         │     │
                                                       │  │ 检测"这轮说完了"   │     │
                                                       │  └────────┬─────────┘     │
                                                       └───────────┼───────────────┘
                                                                   ▼
                                                          主动呼叫 → 电话响铃
```

### 关键设计决策

| 决策 | 原因 |
|---|---|
| **用 FXS 网关而不是"听筒直连声卡"** | 只有网关能给话机提供 **48V 馈电**（否则听筒是死的）、**真拨号音**、**20–25Hz 铃流**（否则不会响铃）。听筒直连声卡只能当麦克风用，铃永远不会响 |
| **音频走 SIP/RTP 而不是 USB 声卡** | 换电脑不用重新接线；网关自己就是电话系统，语义完整 |
| **识别与合成全部本地** | 不需要为说一句话付费，不需要联网，说话内容不出本机 |
| **"回复完成"靠监听会话文件，而不是 Hook** | 当前 AI 助手没有提供可用的 Stop Hook；只读监听会话是唯一不侵入的方案 |

---

## 四、硬件清单

| 件 | 说明 | 价格 | 状态 |
|---|---|---|---|
| 红色座机 | **中诺（CHINO-E）C289 红色**。要点：听筒线是 **4 芯可拔插**（4P4C/RJ9），且为 **2022 年生产**的现代机型（驻极体话筒，可直接工作；80 年代老机的碳精话筒需要改电路） | ¥33 | 已购 |
| FXS 语音网关 | **Grandstream HT801**（中文名"潮流"）。把模拟话机变成 SIP 网络电话；**必须是 FXS 口**（接话机），不是 FXO（接外线） | ¥180 | 已购 |
| USB 网卡 | 给网关单独一个隔离网段（可选，也可以直接把网关接到路由器上） | 已有 | ✅ |
| 网线 | 网关 ↔ 电脑 | ¥5 | 待买 |
| **合计** | | **约 ¥220** | |

> **关于网关怎么选**：任何支持标准 SIP 的 **FXS** 网关都可以。
> 关键要能配三样：**摘机自动拨号**（Off Hook Auto Dial）、**可调的铃流节奏**、**G.711 编码**。
> 双口型号比单口多一个口，可以接第二部电话。

**接线只需三步**：

```
红色座机 ──RJ11 电话线──► 网关 PHONE 1 口
电脑 USB 网卡 ──网线────► 网关 LAN 口
网关 ──电源适配器──────► 插座（必须独立供电）
```

> **为什么不能"只听筒直连声卡"**：话机需要网关提供 **48V 馈电**（否则听筒完全静音）
> 和 **20–25Hz 铃流**（否则永远不会响铃）。这是本项目必须用 FXS 网关的原因——
> 也正是"AI 干完活能打电话叫你"这个核心体验的前提。

---

## 五、软件组成

纯 Node.js（服务）+ Python（识别引擎）+ PowerShell（窗口注入），无重依赖。

| 模块 | 职责 | 行数 |
|---|---|---|
| **音频地基** | G.711 μ-law 编解码、RTP 打包/解析、采样率转换、提示音生成 | ~250 |
| **SIP/RTP 终端** | 来电应答、主动呼叫（回铃）、DTMF 收号、SDP 协商、双向音频 | ~500 |
| **本地识别客户端** | 电话音频 → 文字（走本地离线识别引擎的 WebSocket 协议） | ~230 |
| **电话状态机** | 摘机→就绪音→收音→挂机→识别→提交→回铃 全流程 | ~380 |
| **会话监听器** | 只读监听 AI 助手的会话文件，检测"这一轮回复完成" | ~300 |
| **文字注入** | 把识别结果送进输入框并提交（窗口聚焦 + 剪贴板粘贴） | ~180 |
| **装配入口** | 把上述模块接成可跑服务，含自检/状态/模拟来电 | ~480 |
| **网关配置脚本** | 一键配置网关并支持回滚 | ~180 |

---

## 六、核心工程问题与解决

这一节是项目的技术价值所在。每个问题都是**实际踩出来的**，不是设计时想到的。

### 6.1 音频链路：G.711 μ-law 没有唯一标准表

实现 μ-law 编解码时遇到一个反直觉的坑：网上大量资料说"字节 0x00 表示最大正电平"，
但实测相反，导致一度以为实现有 bug。

**排查方法**：用 Python 标准库 `audioop`（独立权威实现）逐条对照：

| 线性值 | 本实现 / `audioop` |
|---|---|
| +32124 | 0x80 |
| −32124 | 0x00 |
| 0 | **0xFF**（静音） |

**结论**：G.711 μ-law 存在**多种历史变体表**，网上流传的"0x00=最大正电平"属于 AD&D 变体。
**必须用权威实现校准，不能靠查表。**

> 附带收益：用"过零次数"验证重采样后的音高正确性（440Hz 一秒应约 880 次过零），
> 比"能出声就算对"可靠得多。

### 6.2 二进制处理的隐蔽陷阱：`Buffer.from(Float32Array)` 不遵守 byteOffset

送进识别引擎的音频必须转成 base64 的 float32。最初的写法是：

```js
Buffer.from(float32Array).toString('base64')
```

单元测试发现：**本该 1280 字节的音频变成了 3427 字节，解出来的浮点值是 1e31 量级的垃圾。**

**根因**：`Buffer.from(typedArray)` 是"共享同一块内存的视图"，
而 TypedArray 若来自内存池会带非零 `byteOffset`，
于是 `Buffer.from()` 把池中 `byteOffset` 之前的无关内存一起编了进去。

**正确写法**：

```js
const bytes = new Uint8Array(f32.buffer, f32.byteOffset, f32.byteLength);
Buffer.from(bytes).toString('base64');
```

**这个 bug 的可怕之处**：它不会报错、不会崩溃，数据"看起来有内容"，
只是**全是垃圾**——如果不是逐个校验浮点值范围，它会一直潜伏到接上硬件才发现。

### 6.3 读取压缩会话文件：为什么"文件是空的"

需要检测"AI 这一轮回复完成"，方案是只读监听它的会话文件。
第一次尝试时，用 Node 解压得到 **0 字节、2 行**，看起来文件是空的。

**根因**：该会话文件是 **多个 zstd frame 拼接**的，
而 Node 的 `zstdDecompressSync` **只解第一个 frame**。

**解决**：按 zstd 魔数 `28 B5 2F FD` 切分 frame，逐个解压后拼接。
实测 **6166 个 frame 全部成功**，解析出 15 个回合、14 个已完成。

> 另一个发现：Node 的 zstd 对**截断的 frame 是容错的**——不抛错，只返回已解出的部分。
> 所以"正在写入的半截 JSON 行"会被 JSON 解析自然跳过，等写完下次轮询自动补上，
> 不需要任何特殊处理。

### 6.4 如何可靠地识别"这一轮说完了"

会话文件里有两类关键事件：

| 事件 | 含义 |
|---|---|
| `turn/end` 且 `reason.kind == "completed"` | **这一轮结束的可靠标志** |
| `assistant/message` | 助手的产出；一个回合可能有**多条**（分步产出），按序号取**最后一条**才是最终答复 |

**关键设计**：不能见到回合完成就响铃——用户自己打字时也会产生回合，
那样每次敲键盘都会被电话打扰。

**解决**：进入等待状态时记录一个**时间下界**，只对下界之后完成的回合响铃。
实测验证：历史 14 个回合全部被正确排除。

### 6.5 Windows GUI 自动化的两个坑

向输入框注入文字需要：找到窗口 → 聚焦 → 粘贴 → 回车。踩到两个坑：

**坑一：PowerShell 脚本必须全 ASCII。**
Windows PowerShell 5.1 会把**无 BOM 的 UTF-8 文件按 GBK 读取**，
脚本里的中文全部乱码，直接导致语法错误。

**坑二：块注释里不能出现 `#>` 序列。**
它在块注释内部会**提前结束注释**，后面的内容全部被当成代码解析。
（这个坑让我多花了一轮才定位到。）

### 6.6 GPU 环境：`is_available()` 返回 True 不代表能算

在新架构显卡上（sm_120），PyTorch 的 `torch.cuda.is_available()` 仍会返回 `True`，
但如果编译时没有包含对应架构的内核，
**第一次真正执行计算**才会抛出 `no kernel image is available`。

**解决**：写冒烟测试，实测 8 类核函数（matmul fp32/bf16/fp16、conv1d、LayerNorm、softmax、einsum）是否真的能跑，
并检查 `torch.cuda.get_arch_list()` 是否包含本机架构。

### 6.7 一个被放弃的功能：本地语音合成

原计划让 AI 的回复**从听筒里念出来**。实测后放弃。

**实测数据**：本地 TTS 模型（Qwen3-TTS 0.6B）只有 **0.31x 实时率**
——生成 1 秒音频要 3 秒。尝试过的优化与结论：

| 尝试 | 结果 |
|---|---|
| 换注意力实现（manual / sdpa / eager） | 差异仅 ±20%，**不是瓶颈** |
| 换采样参数 | 无效 |
| 定位瓶颈 | 该模型有 **16 个码本，每帧需串行预测 15 次** —— 每秒音频约 180 次前向，**架构上注定慢于实时** |

**根本原因**（调研结论）：
- 该类模型在 Transformers 路径下的 RTF 本就是 2.64（0.38x），
  只有切到 **Linux + vLLM** 才能到 6 倍速——而 vLLM 不支持 Windows；
- 更普遍地，**Windows 的 WDDM 图形调度**让每次 kernel 启动延迟高一个数量级（20–50µs），
  而自回归解码每步有几十个串行小 kernel，开销被成倍放大。

**决策**：放弃听筒播报，回复内容看屏幕即可。
（已调研出替代方案：纯 CPU 的 ONNX 非自回归模型可达 70x 实时率，且部分模型**原生 8kHz 输出**，
正好匹配电话；将来需要时可低成本加回。）

---

## 七、验证方式

### 不依赖硬件的完整闭环验证

项目内置一个"模拟网关"：用软件模拟一台话机**摘机拨号、推送音频、挂机、接听回铃**，
把整条链路真跑一遍。

实测输出：

```
idle → ready（摘机，播就绪音）→ listening
     → transcribing → 识别结果: "……"（时延 137ms）
     → submitting → 已提交 → idle
     → ringing → ★ 发起回铃 → 模拟网关收到 INVITE ✓ → answered
统计: calls=1, submitted=1, rings=1, answered=1
```

**这是本项目最有价值的一环**：它让"接上硬件才发现问题"变成"接上硬件只是最后一步"。

### 测试规模

**146 项单元与集成测试**（`node --test "src/**/*.test.mjs"`），覆盖：

音频编解码与重采样、SDP 生成与解析、SIP 消息解析与畸形输入、
来电/挂机/取消完整流程、**未接听去电的 CANCEL 撤销**、DTMF 收号、
识别协议构造与超时、状态机的每一条边界（录音过短、**整段静音**、识别失败、
空文本、提交失败、超长录音、**按次数响铃**、回铃超时/被拒/总开关）、
会话解析（多 frame、截断帧、乱序消息、**多会话定位**、**锁定等待回合**）、
注入器的错误分类与重试、以及一堆**真机踩过之后补的回归测试**。

另外还有几个面向真机的验证脚本（在 `源码/phone-companion/工具/验证/` 下），
其中 `验证真机响铃.mjs` 会**直接让话机响**（用临时端口，不影响正在跑的服务）：

```bash
node 工具/验证/验证真机响铃.mjs 5        # 让话机响 5 秒
node 工具/验证/验证通知铃.mjs 1          # 验证"响 1 声后自动挂断"
node 工具/验证/验证回铃链路.mjs           # 真状态机 + 假话机，端到端 12 项断言
```


---

## 八、快速开始

需要 **Node ≥ 20**、**Python 3.10+**（只用来配网关），以及一台装好 DSH 的 Windows 机器。

### 1. 接线

```
座机 ──RJ11 电话线──► HT801 的 PHONE 1
HT801 ──网线───────► 电脑的**第二个网口**（建议用 USB 网卡，别占用上网那张）
HT801 ──电源───────► 插座（必须独立供电，它要给话机提供馈电和铃流）
```

### 2. 给那个网口配静态 IP

网关出厂是自己一个网段（常见 `192.168.1.1`），所以电脑那个网口也要在同网段。
Windows：`ncpa.cpl` → 选接网关的那张网卡 → IPv4 → 手动 → 例如 `192.168.1.2 / 255.255.255.0`。

> USB 网卡**拔插后会丢静态 IP**（Windows 会当成新设备重新枚举），症状是网关 ping 不通。
> `工具/` 里有现成的一键修复脚本。

### 3. 准备识别服务

识别用的是 [CapsWriter-Offline](https://github.com/HaujetZhao/CapsWriter-Offline)（本地离线，含 SenseVoice 模型）。
自行 clone 到 `源码/CapsWriter-Offline/`，按它的说明建好 `.venv` 并启动，确认 `127.0.0.1:6016` 在监听。

### 4. 配好本项目的配置

```bash
cd 源码/phone-companion
cp phone.config.example.json phone.config.json
```

改两个必改项：
- `workspace` → **你的 DSH 工作区根目录**（回铃检测靠它定位会话文件，写错会表现为"电话永远不响"）
- `ataAddress` → 你网关的实际地址

### 5. 配网关

```bash
python ht801.py inspect      # 只读：确认型号、固件、逐项核对参数
python ht801.py configure    # 写入（自动备份原值，可 restore 回滚）
```

### 6. 自检 + 启动

```bash
node src/phone-control.mjs selftest    # 端口 / 识别服务 / 会话定位 / 注入窗口 / 提示音 / 网关
node 启动.mjs                          # 一键拉起识别服务 + 电话服务 + 自检
node web/server.mjs                    # 可选：打开网页控制台 http://127.0.0.1:8932
```

然后**拿起听筒** → 听到就绪音（嘀）→ **等它响完再说话** → 放下 → 文字出现在输入框并提交
→ AI 回复完，**电话响一声通知你**（响几声可在控制台里改，默认 1 声后自动挂断）。


---

## 九、已知限制

| 限制 | 说明 |
|---|---|
| **SIP/RTP 无加密** | 没有 TLS、没有认证、媒体不加密。只应运行在**直连或可信隔离网段**（推荐给网关单独一个网口，不接共享路由器） |
| 仅 Windows（服务端） | 窗口注入依赖 Win32 API；SIP/音频/状态机部分是跨平台的 |
| 仅支持一路电话 | 一个网关一个绑定任务 |
| 依赖 AI 助手的会话文件格式 | 会话是内部格式，助手升级后可能失效（已隔离在单个模块内，便于适配） |
| 不处理助手的权限确认 | 只提交文字，不代替用户批准任何操作 |

---

## 十、路线图

- [x] 音频链路（G.711 / RTP）
- [x] SIP 信令（来电 / 回铃 / DTMF / CANCEL）
- [x] 本地离线识别接入
- [x] 电话状态机（摘机→挂机→提交）
- [x] "回复完成"检测与回铃（**按次数**：响 N 声后自动挂断）
- [x] 不依赖硬件的闭环验证
- [x] 网关配置脚本
- [x] **真机联调**（摘机 / 识别 / 注入 / 回铃，已端到端跑通）
- [x] **本地网页控制台**（零依赖）
- [ ] 听筒播报回复 —— **已明确放弃**：本地 TTS 实测只有 0.31x 实时率，
      架构上慢于实时（16 个码本、每帧串行预测 15 次），不是调参能解决的。
      替代方案见 `docs/TTS选型调研-留档.md`
- [ ] 多路电话 / 外壳改造

---

## 十一、开发历程与踩坑

这个项目踩过的坑**比功能还多**，而且大多属于"不报错、但行为全错"的静默故障。
完整记录在 **[docs/开发历程与踩坑记录.md](docs/开发历程与踩坑记录.md)**，其中包括：

- SIP 的 2xx 应答少了 `Contact` 头 → 网关不回 ACK，只听得到忙音
- 通话只靠对端 `BYE` 收尾 → 对端异常消失就永久占线，**之后电话再也不响**
- 没被接听的回铃**从来没发过 CANCEL** → 对端一直振铃、线路一直忙
- 静音时语音识别会**凭空编出一个字**并当成消息提交（已在识别前用电平拦掉）
- 回铃的"哪一轮才算数" —— 一口气修了**四个**同类 bug

---

## 十二、关于这个项目
**它不解决什么大问题。** 它不是效率工具，也不会替代语音助手。

它做的事情很小：**让"跟 AI 说句话"这件事，重新有了拿起和放下的仪式感。**

在一个所有交互都在一块玻璃板上完成的时代，
一个必须**拿起**才能说话、**放下**才算说完、干完活还会**响铃叫你**的东西，
本身就有点好玩。

而且它证明了一件事：**最土的硬件配最硬的技术，可以做出最舒服的体验。**

---

## 十三、致谢

### 思路来源

本项目的**起点是看到有人已经把这件事做成了**：开源项目
**[codex-redline](https://github.com/prikevs/codex-redline)**（MIT）用 Grandstream HT802 把模拟电话
接成了 AI 编程助手的语音终端，摘机说话、挂机提交、完成后响铃。

它证明了这条路走得通，并且公开了最花时间的部分——**网关的全部配置参数**（P 值表）、
**音频参数规格**（G.711 / 8kHz / 20ms 帧 / RFC2833 DTMF）、
以及一批**踩过坑才知道的体验细节**（就绪音频率、回复开头垫静音、响铃次数上限等）。

本项目在其思路基础上做了三处不同的取舍：

| 维度 | codex-redline | 本项目 |
|---|---|---|
| 运行平台 | macOS | Windows |
| 语音识别 / 合成 | 云端 API | **全本地离线** |
| 输入方式 | 系统辅助功能接口 | 窗口聚焦 + 剪贴板注入 |

同时按当前 AI 助手的能力重新设计了"回复完成"的检测方式，并补上了不依赖硬件的闭环验证。

**如果你的环境是 macOS，建议直接使用 codex-redline**——它更完整，且已经跑通实测。

### 其他

音频编解码、SIP/RTP、DTMF 等部分参考了开源社区的通用实现思路，遵守相应开源许可。

---

*本项目全本地运行，语音与文字不出本机。文档中的价格与型号为撰写时的实测值。*

