# ── 运行产物 ──────────────────────────────────────────────
# 状态心跳、服务日志、诊断输出都在这里。含本机路径与通话记录，不该进版本库。
.runtime/

# ── 依赖与虚拟环境 ────────────────────────────────────────
node_modules/
.venv/
venv/
__pycache__/
*.pyc

# ── 本机配置 ──────────────────────────────────────────────
# phone.config.json 里有你自己的工作区路径、网关地址、端口。
# 请复制 phone.config.example.json 改名使用，不要提交真实配置。
phone.config.json

# ── 第三方源码：各自有仓库和许可证，不随本项目分发 ──────────
# 识别引擎请自行 clone（见 README 的"准备识别服务"）
源码/CapsWriter-Offline/
# 参考实现（只作设计参考，未复制其代码）
源码/codex-redline/

# ── 零碎 ──────────────────────────────────────────────────
*.log
# ⚠️ 带斜杠的模式会被锚定到仓库根目录！
#    写 "产物/合成测试-*" 只能匹配 <根>/产物/…，排除不掉 源码/phone-companion/产物/… 里的。
#    用不带斜杠的文件名模式，任何层级都能匹配。
合成测试-*
.DS_Store
Thumbs.db
